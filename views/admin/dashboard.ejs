<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/preline@^2/dist/preline.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body class="bg-gray-100">
    <%- include('../components/sidebar') %>
    <div class="ml-64 p-6">
        <h1 class="text-3xl font-bold mb-6 text-gray-800">Dashboard Admin</h1>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <div class="bg-white rounded-2xl border border-gray-200 p-6 shadow-sm hover:shadow-xl transition-shadow duration-300">
                <div class="flex items-center">
                    <div class="bg-green-100 text-green-600 rounded-full p-3">
                        <i class="fas fa-file-alt fa-2x"></i>
                    </div>
                    <div class="ml-4">
                        <h2 class="text-lg font-semibold text-gray-800">Total Laporan</h2>
                        <p class="text-2xl font-bold text-gray-900">150</p>
                    </div>
                </div>
            </div>  
            <div class="bg-white rounded-2xl border border-gray-200 p-6 shadow-sm hover:shadow-xl transition-shadow duration-300">
                <div class="flex items-center">
                    <div class="bg-blue-100 text-blue-600 rounded-full p-3">
                        <i class="fas fa-check-circle fa-2x"></i>
                    </div>
                    <div class="ml-4">
                        <h2 class="text-lg font-semibold text-gray-800">Laporan Diselesaikan</h2>
                        <p class="text-2xl font-bold text-gray-900">120</p>
                    </div>     
                </div>
            </div>
            <div class="bg-white rounded-2xl border border-gray-200 p-6 shadow-sm hover:shadow-xl transition-shadow duration-300">
                <div class="flex items-center">
                    <div class="bg-red-100 text-red-600 rounded-full p-3">
                        <i class="fas fa-hourglass-half fa-2x"></i>
                    </div>
                    <div class="ml-4">
                        <h2 class="text-lg font-semibold text-gray-800">Laporan Pending</h2>
                        <p class="text-2xl font-bold text-gray-900">30</p>
                    </div>  
                </div>
            </div>
        </div>
        <div class="py-12">
        <div class="bg-green-600 rounded-2xl p-8 mb-10 shadow-lg text-white">
            <div class="flex flex-col md:flex-row justify-between items-center">
                <div>
                    <h1 class="text-3xl lg:text-4xl font-bold mb-2">Selamat Datang!</h1>
                    <p class="text-lg lg:text-xl opacity-90 font-light">Sistem Pelaporan Kehilangan dan Penemuan Barang</p>
                </div>
                <div class="mt-6 md:mt-0">
                    <a href="/admin/reports" class="inline-flex items-center bg-white text-green-600 px-6 py-3 rounded-xl font-semibold hover:bg-gray-100 transition duration-300 transform hover:scale-105 shadow-md">
                       <i class="fas fa-plus mr-2"></i>Buat Laporan Baru
                    </a>
                </div>
            </div>
        </div>

      <h1 class="text-3xl font-bold mb-6 text-gray-800">
        Verifikasi Laporan
      </h1>

      <div class="space-y-6">
        <% reports.forEach(report => { %>
        <!-- Card -->
        <div
          class="flex bg-white rounded-xl shadow-md overflow-hidden"
        >
          <!-- Foto barang -->
          <img
            src="/uploads/<%= report.foto_barang %>"
            alt="<%= report.nama_barang %>"
            class="w-48 h-48 object-cover"
          />

          <!-- Info -->
          <div class="flex-1 p-4 flex flex-col justify-between">
            <div>
              <h3 class="text-xl font-bold text-gray-900">
                <%= report.nama_barang %>
              </h3>
              <p class="text-gray-600">
                Dibuat oleh: <%= report.User.nama %>
              </p>
              <p class="text-sm text-gray-500">
                Jenis laporan: <%= report.jenis_laporan %>
              </p>
              <span
                class="inline-block mt-2 px-3 py-1 text-xs font-semibold rounded-full bg-yellow-100 text-yellow-800"
              >
                <%= report.status %>
              </span>
            </div>

            <!-- Tombol buka modal -->
            <button
              type="button"
              class="mt-4 bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg font-semibold"
              data-hs-overlay="#verifyModal-<%= report.id_laporan %>"
            >
              Verifikasi
            </button>
          </div>
        </div>

        <!-- Modal Verifikasi -->
        <div
          id="verifyModal-<%= report.id_laporan %>"
          class="hs-overlay hidden fixed top-0 left-0 z-50 w-full h-full flex items-center justify-center bg-black/50"
          role="dialog"
          tabindex="-1"
        >
          <div class="bg-white rounded-xl shadow-md w-full max-w-md">
            <!-- Header -->
            <div class="py-3 px-4 border-b border-gray-200">
              <h3 class="font-bold text-gray-800">
                Detail Laporan
              </h3>
            </div>

            <!-- Body -->
            <div class="p-4 space-y-2">
              <img
                src="/uploads/<%= report.foto_barang %>"
                alt="<%= report.nama_barang %>"
                class="rounded-lg w-full object-cover mb-4"
              />
              <p><strong>Nama pelapor:</strong> <%= report.User.nama %></p>
              <p><strong>Nama barang:</strong> <%= report.nama_barang %></p>
              <p><strong>Jenis laporan:</strong> <%= report.jenis_laporan %></p>
              <p>
                <strong>Tanggal laporan:</strong>
                <%= report.tanggal_laporan.toLocaleDateString("id-ID") %>
              </p>
              <p><strong>Lokasi:</strong> <%= report.lokasi %></p>
              <p><strong>Deskripsi:</strong> <%= report.deskripsi %></p>
            </div>

            <!-- Footer -->
            <form
              method="POST"
              action="/admin/verifikasi/<%= report.id_laporan %>"
              class="flex flex-col items-end gap-3 py-3 px-4 border-t border-gray-200"
            >
              <p class="text-sm text-gray-700 mr-auto">
                Setujui laporan ini?
              </p>
              <div class="flex gap-2">
                <button
                  name="action"
                  value="reject"
                  type="submit"
                  class="py-2 px-3 text-sm font-medium rounded-lg bg-red-600 text-white hover:bg-red-700"
                >
                  Tidak
                </button>
                <button
                  name="action"
                  value="approve"
                  type="submit"
                  class="py-2 px-3 text-sm font-medium rounded-lg bg-green-600 text-white hover:bg-green-700"
                >
                  Ya
                </button>
              </div>
            </form>
          </div>
        </div>
        <% }) %>
      </div>
    </div>
    </div>  
</body>
</html>