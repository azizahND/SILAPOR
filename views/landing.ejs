<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SILAPOR - Universitas Andalas</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = { darkMode: "class" };
    </script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>

  <body
    class="font-[Inter,sans-serif] antialiased bg-[conic-gradient(at_top_right,_var(--tw-gradient-stops))] from-[#eef2ff] via-white to-[#dcfce7] dark:from-gray-900 dark:via-gray-800 dark:to-gray-900 transition-colors duration-500 min-h-screen flex flex-col"
  >
    <!-- 🌗 Tombol toggle dark mode -->
    <button
      id="theme-toggle"
      class="fixed top-6 right-6 z-50 p-3 rounded-full bg-white/80 dark:bg-gray-800/80 backdrop-blur-lg border border-gray-200 dark:border-gray-700 shadow-lg hover:shadow-xl transition-all duration-300 group"
      aria-label="Toggle dark mode"
    >
      <svg
        id="theme-toggle-dark-icon"
        class="w-5 h-5 text-gray-700 dark:text-gray-300 hidden dark:block"
        fill="currentColor"
        viewBox="0 0 20 20"
      >
        <path
          d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"
        ></path>
      </svg>
      <svg
        id="theme-toggle-light-icon"
        class="w-5 h-5 text-gray-700 dark:text-gray-300 block dark:hidden"
        fill="currentColor"
        viewBox="0 0 20 20"
      >
        <path
          fill-rule="evenodd"
          d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1z"
          clip-rule="evenodd"
        ></path>
      </svg>
    </button>

    <!-- 🏫 Hero Section -->
    <header
      class="flex flex-col items-center justify-center text-center px-6 py-24 relative overflow-hidden"
    >
      <img
        src="/images/logo.png"
        alt="Logo"
        class="w-28 h-28 mb-6 animate-[float_6s_ease-in-out_infinite]"
      />
      <h1
        class="text-4xl lg:text-5xl font-extrabold bg-gradient-to-r from-green-700 to-green-500 dark:from-green-300 dark:to-green-400 bg-clip-text text-transparent mb-4"
      >
        Sistem Informasi Laporan Penemuan & Kehilangan
      </h1>
      <p
        class="text-gray-700 dark:text-gray-300 text-lg max-w-2xl leading-relaxed mb-8"
      >
        Layanan resmi Universitas Andalas untuk membantu mahasiswa, dosen, dan
        staf menemukan serta melaporkan barang yang hilang di lingkungan kampus.
      </p>

      <div class="flex flex-wrap justify-center gap-4">
        <a
          href="/login"
          class="bg-gradient-to-r from-[#16a34a] to-[#15803d] hover:from-[#15803d] hover:to-[#166534] text-white font-semibold px-8 py-4 rounded-xl shadow-lg hover:shadow-xl transition-all duration-300"
        >
          Masuk Sekarang
        </a>
        <a
          href="/register"
          class="px-8 py-4 rounded-xl border-2 border-green-600 text-green-700 dark:text-green-300 hover:bg-green-50 dark:hover:bg-green-900/30 transition-all font-semibold"
        >
          Daftar Akun
        </a>
      </div>

      <img
        src="/images/unand.jpg"
        alt="Gedung Universitas"
        class="absolute top-0 left-0 w-full h-full object-cover opacity-10 dark:opacity-20 -z-10"
      />
    </header>

    <!-- 💡 Fitur -->
    <section class="px-6 py-16 bg-white/70 dark:bg-gray-800/70 backdrop-blur-md">
      <div class="max-w-6xl mx-auto text-center">
        <h2
          class="text-3xl font-bold text-gray-900 dark:text-white mb-8 bg-gradient-to-r from-gray-900 to-gray-600 dark:from-white dark:to-gray-300 bg-clip-text text-transparent"
        >
          Mengapa Menggunakan SILAPOR?
        </h2>
        <div
          class="grid grid-cols-1 md:grid-cols-3 gap-8 text-gray-700 dark:text-gray-300"
        >
          <div
            class="p-6 bg-white dark:bg-gray-900 rounded-2xl shadow hover:shadow-xl transition"
          >
            <svg
              class="w-10 h-10 mb-4 text-green-600 dark:text-green-400 mx-auto"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M13 16h-1v-4h-1m0-4h.01M12 18.5c4.142 0 7.5-3.358 7.5-7.5S16.142 3.5 12 3.5 4.5 6.858 4.5 11s3.358 7.5 7.5 7.5z"
              />
            </svg>
            <h3 class="font-semibold text-lg mb-2">Transparan & Terpercaya</h3>
            <p>Setiap laporan diverifikasi dan dapat dipantau oleh pengguna.</p>
          </div>

          <div
            class="p-6 bg-white dark:bg-gray-900 rounded-2xl shadow hover:shadow-xl transition"
          >
            <svg
              class="w-10 h-10 mb-4 text-green-600 dark:text-green-400 mx-auto"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M5 13l4 4L19 7"
              />
            </svg>
            <h3 class="font-semibold text-lg mb-2">Mudah Digunakan</h3>
            <p>Proses pelaporan dan penemuan barang hanya butuh beberapa klik.</p>
          </div>

          <div
            class="p-6 bg-white dark:bg-gray-900 rounded-2xl shadow hover:shadow-xl transition"
          >
            <svg
              class="w-10 h-10 mb-4 text-green-600 dark:text-green-400 mx-auto"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 8v4l3 3m6 0a9 9 0 11-18 0 9 9 0 0118 0z"
              />
            </svg>
            <h3 class="font-semibold text-lg mb-2">Cepat Tanggap</h3>
            <p>Notifikasi otomatis membantu barang cepat ditemukan kembali.</p>
          </div>
        </div>
      </div>
    </section>

   <section class="px-6 py-16 bg-white/70 dark:bg-gray-800/70 backdrop-blur-md">
  <div class="max-w-6xl mx-auto text-start">
    <h2
      class="text-3xl text-center font-bold text-gray-900 dark:text-white mb-8 bg-gradient-to-r from-gray-900 to-gray-600 dark:from-white dark:to-gray-300 bg-clip-text text-transparent"
    >
      Laporan Penemuan Barang
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <% laporanPenemuan.forEach(item => { %>
        <div
          class="p-6 bg-white dark:bg-gray-900 rounded-2xl shadow hover:shadow-xl transition-all duration-300"
        >
          <img
            src="/uploads/<%= item.foto_barang %>"
            alt="<%= item.nama_barang %>"
            class="w-full h-48 object-cover rounded-xl mb-3"
          />
          <h3 class="font-semibold text-lg text-gray-900 dark:text-gray-100 mb-1">
            <%= item.nama_barang %>
          </h3>
          <p class="text-sm text-gray-600 dark:text-gray-400 mb-1">
            Lokasi: <%= item.lokasi %>
          </p>
          <p class="text-sm text-gray-500 dark:text-gray-500 mb-3">
            Dilaporkan oleh: <%= item.User?.nama || 'Anonim' %>
          </p>

          <!-- Tombol Lihat Detail -->
          <button
            onclick="openModal('<%= item.id_laporan %>')"
            class="px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg text-sm transition"
          >
            Lihat Detail
          </button>

          <!-- Modal -->
          <div
            id="modal-<%= item.id_laporan %>"
            class="fixed inset-0 hidden bg-black/50 backdrop-blur-sm flex items-center justify-center z-50"
          >
            <div
              class="bg-white dark:bg-gray-900 rounded-2xl shadow-lg w-11/12 md:w-1/2 p-6 relative"
            >
              <button
                onclick="closeModal('<%= item.id_laporan %>')"
                class="absolute top-3 right-3 text-gray-400 hover:text-gray-700 dark:hover:text-gray-300"
              >
                ✖
              </button>

              <img
                src="/uploads/<%= item.foto_barang %>"
                alt="<%= item.nama_barang %>"
                class="w-full h-64 object-cover rounded-xl mb-4"
              />
              <h3 class="text-2xl font-semibold text-gray-900 dark:text-white mb-2">
                <%= item.nama_barang %>
              </h3>
              <p class="text-gray-700 dark:text-gray-300 mb-1">
                <strong>Lokasi:</strong> <%= item.lokasi %>
              </p>
              <p class="text-gray-700 dark:text-gray-300 mb-1">
                <strong>Tanggal Penemuan:</strong>
                <%= item.tanggal_kejadian?.toLocaleDateString('id-ID') || '-' %>
              </p>
              <p class="text-gray-700 dark:text-gray-300 mb-3">
                <strong>Deskripsi:</strong> <%= item.deskripsi || 'Tidak ada deskripsi' %>
              </p>

              <!-- Tombol Klaim -->
              <a
                href="/login"
                class="inline-block mt-3 px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg text-sm transition"
              >
                Klaim Barang
              </a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<script>
  function openModal(id) {
    document.getElementById("modal-" + id).classList.remove("hidden");
  }
  function closeModal(id) {
    document.getElementById("modal-" + id).classList.add("hidden");
  }
</script>


<!-- 🔍 Laporan Kehilangan Barang -->
<section class="px-6 py-16 bg-white/70 dark:bg-gray-800/70 backdrop-blur-md">
  <div class="max-w-6xl mx-auto text-center">
    <h2
      class="text-3xl font-bold text-gray-900 mb-10"
    >
      Laporan Kehilangan Barang
    </h2>

    <% if (laporanKehilangan && laporanKehilangan.length > 0) { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        <% laporanKehilangan.forEach(item => { %>
          <div
            class="group bg-white dark:bg-gray-900 rounded-2xl shadow-md hover:shadow-xl transition-all duration-300 overflow-hidden"
          >
            <div class="overflow-hidden">
              <img
                src="/uploads/<%= item.foto_barang %>"
                alt="<%= item.nama_barang %>"
                class="w-full h-52 object-cover rounded-t-2xl group-hover:scale-105 transition-transform duration-300"
              />
            </div>

            <div class="p-5 text-left">
              <h3 class="font-semibold text-lg text-gray-900 dark:text-gray-100 mb-1">
                <%= item.nama_barang %>
              </h3>
              <p class="text-sm text-gray-600 dark:text-gray-400 mb-1">
                Lokasi: <span class="font-medium"><%= item.lokasi %></span>
              </p>
              <p class="text-sm text-gray-500 dark:text-gray-500">
                Dilaporkan oleh: <%= item.User?.nama || 'Anonim' %>
              </p>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p class="text-gray-500 dark:text-gray-400 text-center">
        Belum ada laporan kehilangan yang tersedia.
      </p>
    <% } %>
  </div>
</section>




    <!-- 🧩 Footer -->
    <footer
      class="py-6 bg-gray-50 dark:bg-gray-900 text-center text-gray-600 dark:text-gray-400 border-t border-gray-200 dark:border-gray-700"
    >
      <p>
        © 2025 SILAPOR - Universitas Andalas |
        <span class="text-green-600 dark:text-green-400 font-semibold"
          >Dibangun oleh Neo Tech</span
        >
      </p>
    </footer>

    <script>
        function openModal(id) {
            document.getElementById("modal-" + id).classList.remove("hidden");
        }
        function closeModal(id) {
            document.getElementById("modal-" + id).classList.add("hidden");
        }


      document.addEventListener("DOMContentLoaded", function () {
        const themeToggle = document.getElementById("theme-toggle");
        const htmlElement = document.documentElement;
        const savedTheme = localStorage.getItem("theme") || "light";
        htmlElement.classList.toggle("dark", savedTheme === "dark");

        themeToggle.addEventListener("click", function () {
          htmlElement.classList.toggle("dark");
          const currentTheme = htmlElement.classList.contains("dark")
            ? "dark"
            : "light";
          localStorage.setItem("theme", currentTheme);
        });
      });
    </script>
  </body>
</html>